name: midnight_oracle_backend

services:
  db:
    image: postgres:17
    container_name: postgres
    ports:
      - "5432:5432"
    # Se a gente quiser a opção de deletar o container sem perder os dados
    # volumes:
    #   - postgres_devweb:/var/lib/postgresql/data
    env_file:
      - .env

  squirrel:
    build: .
    profiles:
      - gen
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - type: bind
        source: ./src/
        target: /server/src
    command: ["-m", "squirrel"]

  cigogne:
    build: .
    profiles:
      - migrate
    env_file:
      - .env
    depends_on:
      - db
    command: [ "-m", "cigogne", "all" ]
