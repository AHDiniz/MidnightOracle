info:
  name: Category - Create
  type: http
  seq: 13

http:
  method: POST
  url: http://127.0.0.1:8910/category
  body:
    type: json
    data: |-
      {
        "name": "Ragebait"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
