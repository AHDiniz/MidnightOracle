info:
  name: Feed - Update
  type: http
  seq: 6

http:
  method: POST
  url: http://127.0.0.1:8910/feed/update/
  body:
    type: json
    data: |-
      {
        "feed_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
