info:
  name: Category/Feed - Create
  type: http
  seq: 15

http:
  method: POST
  url: http://127.0.0.1:8910/category/feed/2
  body:
    type: json
    data: |-
      {
        "category_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
