info:
  name: Auth - Register
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8910/auth/register/
  body:
    type: json
    data: |
      {
        "username": "nome",
        "email": "a@mail.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
