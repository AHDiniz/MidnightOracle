info:
  name: Feed - Delete
  type: http
  seq: 7

http:
  method: DELETE
  url: http://127.0.0.1:8910/feed/delete/1/
  body:
    type: json
    data: |-
      {
        "feed_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
